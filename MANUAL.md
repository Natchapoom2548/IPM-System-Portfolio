# คู่มือระบบ Portfolio Demo

## Portfolio Demo - Fictional Data

ระบบนี้เป็นผลงานสาธิตสำหรับ Portfolio เท่านั้น ข้อมูลหน่วยงาน บุคคล
เครื่องมือแพทย์ เลขครุภัณฑ์ และประวัติทั้งหมดเป็นข้อมูลสมมติ

ระบบไม่เชื่อมต่อฐานข้อมูล ระบบงานจริง หรือ private environment ใด ๆ

## วิธีเข้าสู่ระบบ

เลือกบัญชีสาธิตจากหน้า Login และใช้รหัสผ่านเดียวกันทุกบัญชี:

```text
Demo123!
```

| Username | สิทธิ์สาธิต |
| --- | --- |
| `demo_admin` | ผู้ดูแลระบบ |
| `demo_registration` | ฝ่ายลงทะเบียน |
| `demo_ipm` | ฝ่ายตรวจสอบ IPM |
| `demo_repair` | ฝ่ายซ่อม |
| `demo_reporting` | ฝ่ายรายงานผล |

## การจัดเก็บข้อมูล

- ข้อมูลเริ่มต้นมาจาก `src/data/mockData.ts`
- การแก้ไขชั่วคราวเก็บใน `localStorage` ของ browser
- Refresh แล้วข้อมูลชั่วคราวยังคงอยู่ใน browser เดิม
- กด **Reset demo** ที่หน้า Login เพื่อคืนข้อมูลเริ่มต้น
- ข้อมูลไม่ถูกส่งไปยังฐานข้อมูลหรือบริการภายนอก

## Workflow สาธิต

1. **Registration** — ลงทะเบียนและนำเข้าครุภัณฑ์สมมติ
2. **IPM** — บันทึกผลตรวจเชิงคุณภาพและเชิงปริมาณ
3. **Repair** — จำลองการวิเคราะห์และปิดงานซ่อม
4. **Reporting** — ออกใบรับรอง พิมพ์รายงาน และดาวน์โหลด Demo JSON
5. **Dashboard/Search** — ดูภาพรวมและค้นหาประวัติข้อมูลสมมติ

## Vercel

สามารถ deploy ได้โดยไม่ตั้งค่า environment variables:

```bash
npm install
npm run build
```

Vercel API เป็น mock-only และใช้หน่วยความจำชั่วคราว ส่วนการใช้งานผ่าน UI
ใช้ `localStorage` เพื่อให้ refresh มีพฤติกรรมที่คาดเดาได้
