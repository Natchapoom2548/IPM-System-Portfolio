# Portfolio Demo - Fictional Data
# No environment variables are required.
#
# The browser uses localStorage for temporary demo changes, and the Vercel API
# exposes mock in-memory responses only. Do not add database URLs, API keys,
# production credentials, or private environment values to this demo.
# DATABASE_URL, Supabase, Firebase, and private authentication providers are
# intentionally unsupported in this public repository.
